{
  "name": "gaia-challenge-fullstack",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=api",
    "dev:web": "npm run dev --workspace=web",
    "type-check": "npm run type-check --workspaces --if-present"
  }
}
