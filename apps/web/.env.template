# Prefer a single repo-root `.env` (see /.env.template).
# If you use this file instead, copy it to `.env` in this directory.

# JWT for Gaia stage GraphQL. Raw token or full "Bearer <token>" header value.
GRAPHQL_AUTH=
